# Producer ingest contract redirect

This feature-local file is an internal traceability stub, not a published
producer contract.

The normative producer-facing contract is:

- Package: [`otel-ingest`](../../../contracts/public/otel-ingest/README.md)
- Version: [`1.0.0`](../../../contracts/public/otel-ingest/VERSION)
- Release tag: `contracts/otel-ingest/v1.0.0`

Producer repositories MUST pin the released public package and MUST NOT import
Feature 001 or other files under this repository's `specs/` tree.

